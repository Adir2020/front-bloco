# bloco frontend

# 1 passo executar o comando (yarn) para instalar as dependecias.

# 2 passo executar o comando (yarn) para rodar a parte frontal e a mesma se comunicar com a parte do backend, aplicativo aberto na porta 8080.